# 🎨 코딩 컨벤션

> **대상 독자**: 팀 전원
> **목적**: 4명이 짠 코드가 한 사람이 짠 것처럼 보이게 한다
> **버전**: v1.0 (2026-05-19)

---

## 0. 핵심 원칙 (TL;DR)

1. **포맷은 도구에게**: Prettier가 자동. 사람은 로직에 집중.
2. **이름은 의도를 드러내라**: `data`, `temp`, `foo` 금지. `userProfile`, `noteList` OK.
3. **타입 없이 export 금지**: TypeScript의 모든 public API는 타입 명시.
4. **에러는 무시 X**: `try-catch` 없이 비동기 호출 금지.
5. **주석은 WHY**: "무엇을 하는가"는 코드가 말한다. "왜 그렇게 했는가"만 적는다.

---

## 1. 자동화 도구 (사람이 신경 쓰지 말 것)

### 1.1 Prettier (포맷)

`/.prettierrc.json` (Front, Backend 공통):

```json
{
  "semi": true,
  "singleQuote": true,
  "trailingComma": "all",
  "printWidth": 100,
  "tabWidth": 2,
  "useTabs": false,
  "arrowParens": "always",
  "endOfLine": "lf"
}
```

### 1.2 ESLint (린트)

#### Front (`Front/.eslintrc.json`)

```json
{
  "extends": [
    "next/core-web-vitals",
    "plugin:@typescript-eslint/recommended",
    "plugin:react-hooks/recommended",
    "prettier"
  ],
  "rules": {
    "@typescript-eslint/no-unused-vars": ["error", { "argsIgnorePattern": "^_" }],
    "@typescript-eslint/no-explicit-any": "warn",
    "react/jsx-key": "error",
    "no-console": ["warn", { "allow": ["warn", "error"] }],
    "prefer-const": "error",
    "eqeqeq": ["error", "always"]
  }
}
```

#### Backend (`Backend/.eslintrc.json`)

```json
{
  "extends": [
    "eslint:recommended",
    "plugin:@typescript-eslint/recommended",
    "prettier"
  ],
  "rules": {
    "@typescript-eslint/no-unused-vars": ["error", { "argsIgnorePattern": "^_" }],
    "no-console": ["warn", { "allow": ["warn", "error"] }],
    "prefer-const": "error",
    "eqeqeq": ["error", "always"]
  }
}
```

### 1.3 EditorConfig (`.editorconfig` 리포 루트)

```ini
root = true

[*]
indent_style = space
indent_size = 2
end_of_line = lf
charset = utf-8
trim_trailing_whitespace = true
insert_final_newline = true

[*.md]
trim_trailing_whitespace = false
```

### 1.4 Husky + lint-staged (커밋 전 자동 검사)

`package.json` (각 패키지):

```json
{
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "typecheck": "tsc --noEmit"
  },
  "lint-staged": {
    "*.{ts,tsx}": ["eslint --fix", "prettier --write"],
    "*.{js,jsx,json,md}": ["prettier --write"]
  }
}
```

→ 커밋 시 자동 포맷·린트. 사람은 신경 X.

### 1.5 VSCode 추천 설정 (`.vscode/settings.json`)

```json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": "explicit"
  },
  "typescript.tsdk": "node_modules/typescript/lib"
}
```

---

## 2. TypeScript 공통

### 2.1 타입 vs 인터페이스

**원칙**: `type` 우선 사용. `interface`는 **확장이 필요한 클래스 계약**에만.

```typescript
// ✅
type User = {
  id: string;
  email: string;
};

type AdminUser = User & { role: 'admin' };

// ⚠️ interface는 다음과 같은 경우에만
interface IRepository<T> {
  findById(id: string): Promise<T | null>;
}
class NoteRepository implements IRepository<Note> { ... }
```

### 2.2 `any` 금지 (`unknown` 사용)

```typescript
// ❌
function parse(input: any) { ... }

// ✅
function parse(input: unknown) {
  if (typeof input === 'string') { ... }
}

// ✅ 외부 라이브러리가 any 반환 시
const data = (await axios.get('/x')).data as User;
```

### 2.3 함수 시그니처는 항상 명시

```typescript
// ❌
function createNote(title, content) { ... }

// ✅
function createNote(title: string, content: string): Promise<Note> { ... }
```

### 2.4 nullable 처리

```typescript
// ✅ 명시적 null
type Folder = {
  id: string;
  parent_id: string | null;
};

// ❌ undefined와 null 섞기
type Bad = {
  parent_id?: string | null;  // 두 가지 의미?
};
```

---

## 3. 명명 규칙 (Naming)

### 3.1 케이스

| 종류 | 케이스 | 예시 |
|---|---|---|
| **변수·함수** | `camelCase` | `noteList`, `getCurrentUser` |
| **타입·클래스·컴포넌트** | `PascalCase` | `NoteEditor`, `User` |
| **상수** | `UPPER_SNAKE_CASE` | `MAX_NOTE_LENGTH = 50000` |
| **파일 (컴포넌트)** | `PascalCase.tsx` | `NoteEditor.tsx` |
| **파일 (일반 ts)** | `camelCase.ts` | `noteService.ts` |
| **폴더** | `kebab-case` | `link-parser/` |
| **CSS 클래스** | `kebab-case` (또는 Tailwind) | `note-card`, `bg-blue-500` |
| **DB 필드** | `snake_case` | `user_id`, `created_at` |
| **API URL** | `kebab-case` 복수형 | `/api/v1/notes`, `/folders/:id` |

### 3.2 의미 있는 이름

✅:
```typescript
const filteredActiveNotes = notes.filter((n) => !n.is_archived);
function findUserByEmail(email: string): Promise<User | null> { ... }
```

❌:
```typescript
const list = notes.filter((n) => !n.is_archived);   // 무슨 list?
function find(e: string) { ... }                      // 무엇을 find?
```

### 3.3 Boolean 이름

`is`, `has`, `can`, `should` 접두사:

```typescript
const isLoggedIn = !!user;
const hasPermission = checkAcl(...);
const canEdit = user.id === note.user_id;
const shouldRetry = error.code === 'TIMEOUT';
```

### 3.4 함수 동사

| 동사 | 의미 |
|---|---|
| `get` | 동기 조회 |
| `fetch` | 비동기 조회 (네트워크) |
| `find` | 검색 (없으면 null) |
| `create` / `add` | 생성 |
| `update` / `set` | 수정 |
| `delete` / `remove` | 삭제 |
| `parse` | 변환 |
| `validate` | 검증 |
| `handle` | 이벤트 처리 (`handleClick`) |
| `on` | 이벤트 props (`onClick`) |

---

## 4. Frontend (React / Next.js)

### 4.1 컴포넌트 작성 규칙

```tsx
// ✅ 1파일 1컴포넌트, named export, props 타입 inline
type NoteCardProps = {
  note: Note;
  onClick: (id: string) => void;
};

export function NoteCard({ note, onClick }: NoteCardProps) {
  return (
    <article
      className="cursor-pointer rounded-lg border p-4 hover:shadow-md"
      onClick={() => onClick(note.id)}
    >
      <h3 className="text-lg font-semibold">{note.title}</h3>
      <p className="mt-2 line-clamp-2 text-sm text-gray-600">{note.preview}</p>
    </article>
  );
}
```

```tsx
// ❌ default export, 타입 누락, 인라인 스타일
export default function NoteCard(props) {
  return <div style={{ padding: 16 }} onClick={props.onClick}>{props.note.title}</div>;
}
```

### 4.2 Hook 규칙

```tsx
// ✅ 커스텀 훅은 use 접두사
function useNotes(folderId?: string) {
  const { data, error, isLoading } = useSWR(
    folderId ? `/notes?folder_id=${folderId}` : '/notes',
    notesApi.list
  );
  return { notes: data?.items ?? [], error, isLoading };
}
```

훅 규칙:
- 조건문 안에서 호출 금지
- 컴포넌트 최상단에 모아두기
- 의존성 배열 정확히 (`exhaustive-deps` 린트 활성)

### 4.3 Props 컨벤션

```tsx
// 이벤트 핸들러: on{Event}
type ButtonProps = {
  onClick?: () => void;
  onDoubleClick?: () => void;
};

// children은 명시
type ModalProps = {
  open: boolean;
  children: React.ReactNode;
};

// optional은 ? + default
function Button({ size = 'md', variant = 'primary', children }: ButtonProps) { ... }
```

### 4.4 폴더 구조 일관성

각 feature 폴더는 다음 구조:

```
features/editor/
├── NoteEditor.tsx              # 메인 컴포넌트
├── MarkdownPreview.tsx
├── AutocompleteDropdown.tsx
├── hooks/
│   └── useAutocomplete.ts
├── utils/
│   └── insertAtCursor.ts
└── index.ts                    # re-export
```

### 4.5 스타일링 (Tailwind)

- 클래스 길어지면 `clsx` 또는 `cn` 헬퍼 사용
- 컴포넌트 분리하기 (한 element에 클래스 10개 넘으면)

```tsx
import { clsx } from 'clsx';

<button className={clsx(
  'rounded-md px-4 py-2 font-medium transition',
  variant === 'primary' && 'bg-blue-500 text-white hover:bg-blue-600',
  variant === 'secondary' && 'bg-gray-100 text-gray-900 hover:bg-gray-200',
  disabled && 'cursor-not-allowed opacity-50',
)}>
  {children}
</button>
```

### 4.6 import 순서

```tsx
// 1) 외부 라이브러리
import { useState } from 'react';
import { useRouter } from 'next/navigation';

// 2) 내부 (절대 경로 @ 또는 상대)
import { Button } from '@/components/ui/Button';
import { useAuthStore } from '@/store/authStore';

// 3) 같은 폴더
import { NoteCard } from './NoteCard';

// 4) 타입 (별도)
import type { Note } from '@/types/note';

// 5) 스타일
import './globals.css';
```

### 4.7 조건부 렌더링

```tsx
// ✅
{isLoading && <Spinner/>}
{error && <ErrorState onRetry={retry}/>}
{!isLoading && !error && notes.length === 0 && <EmptyState/>}
{!isLoading && !error && notes.map((n) => <NoteCard key={n.id} note={n} onClick={...}/>)}
```

→ 패턴이 많으면 컴포넌트로 분리 (`<AsyncList state={...}/>`).

---

## 5. Backend (Node.js / Express)

### 5.1 레이어 책임 분리

```typescript
// ❌ 라우트에 다 때려박기
router.post('/notes', async (req, res) => {
  const { title, content } = req.body;
  if (!title) return res.status(400).send('title required');
  const user = jwt.verify(req.headers.authorization, SECRET);
  const note = await db.collection('notes').insertOne({ title, content, user_id: user.id });
  res.json(note);
});
```

```typescript
// ✅ 레이어 분리
// routes/noteRoutes.ts
router.post('/', authGuard, validate(createNoteSchema), noteController.create);

// controllers/noteController.ts
export const noteController = {
  create: async (req, res, next) => {
    try {
      const note = await noteService.create(req.user.id, req.body);
      res.status(201).json({ success: true, data: note });
    } catch (err) {
      next(err);
    }
  },
};

// services/noteService.ts
export const noteService = {
  create: async (userId: string, data: CreateNoteDto): Promise<Note> => {
    const parsed = linkParserService.parse(data.content);
    return noteRepo.create({ ...data, user_id: userId, nodes: parsed.nodes });
  },
};

// repositories/noteRepo.ts
export const noteRepo = {
  create: (data: NoteCreate): Promise<Note> => Document.create(data),
};
```

### 5.2 에러 처리

```typescript
// utils/AppError.ts
export class AppError extends Error {
  constructor(
    public statusCode: number,
    public code: string,
    message: string,
    public details?: unknown,
  ) {
    super(message);
  }
}

// services에서
if (!note) throw new AppError(404, 'NOT_FOUND', '노트를 찾을 수 없습니다.');
if (note.user_id !== userId) throw new AppError(403, 'FORBIDDEN', '권한이 없습니다.');

// middlewares/errorHandler.ts
export const errorHandler: ErrorRequestHandler = (err, req, res, next) => {
  if (err instanceof AppError) {
    return res.status(err.statusCode).json({
      success: false,
      error: { code: err.code, message: err.message, details: err.details },
    });
  }
  console.error(err);
  res.status(500).json({
    success: false,
    error: { code: 'INTERNAL_ERROR', message: '서버 오류가 발생했습니다.' },
  });
};
```

### 5.3 zod 검증

```typescript
// schemas/noteSchema.ts
import { z } from 'zod';

export const createNoteSchema = z.object({
  title: z.string().min(1).max(200),
  content: z.string().max(50_000),
  folder_id: z.string().regex(/^[a-f\d]{24}$/i).nullable().optional(),
});

export type CreateNoteDto = z.infer<typeof createNoteSchema>;

// middlewares/validate.ts
export const validate = (schema: z.ZodSchema) =>
  (req: Request, _res: Response, next: NextFunction) => {
    const result = schema.safeParse(req.body);
    if (!result.success) {
      return next(new AppError(400, 'VALIDATION_ERROR', '입력값이 올바르지 않습니다.', result.error.format()));
    }
    req.body = result.data;
    next();
  };
```

### 5.4 비동기 코드

```typescript
// ✅ async/await + try-catch
async function getNote(id: string): Promise<Note> {
  try {
    const note = await Document.findById(id);
    if (!note) throw new AppError(404, 'NOT_FOUND', 'Not found');
    return note;
  } catch (err) {
    if (err instanceof AppError) throw err;
    throw new AppError(500, 'INTERNAL_ERROR', 'DB error', err);
  }
}

// ❌ Promise then 체인 (가독성 떨어짐)
function getNote(id) {
  return Document.findById(id).then((n) => n).catch((e) => null);
}
```

### 5.5 환경변수

```typescript
// config/env.ts
import { z } from 'zod';

const envSchema = z.object({
  NODE_ENV: z.enum(['development', 'production', 'test']),
  PORT: z.coerce.number().default(4000),
  MONGO_URI: z.string().url(),
  JWT_SECRET: z.string().min(32),
  JWT_EXPIRES_IN: z.string().default('7d'),
  CORS_ORIGIN: z.string().url(),
});

export const env = envSchema.parse(process.env);
// 사용: env.MONGO_URI (타입 안전)
```

> 🚨 `process.env`를 직접 참조 X. 위처럼 한 곳에서 검증 후 export.

### 5.6 로깅

```typescript
import winston from 'winston';

export const logger = winston.createLogger({
  level: env.NODE_ENV === 'production' ? 'info' : 'debug',
  format: winston.format.combine(
    winston.format.timestamp(),
    winston.format.json(),
  ),
  transports: [
    new winston.transports.Console(),
    new winston.transports.File({ filename: 'logs/error.log', level: 'error' }),
  ],
});

// 사용
logger.info('User logged in', { userId });
logger.error('DB connection failed', { error: err.message });
```

> 🚫 `console.log` 사용 금지 (lint 경고). 디버그용은 `logger.debug`.

---

## 6. 주석 (Comments)

### 6.1 좋은 주석 (WHY)

```typescript
// ✅
// MongoDB는 트랜잭션에서 같은 session을 모든 쿼리에 전달해야 한다.
// 누락하면 트랜잭션 밖에서 실행되어 격리가 깨진다.
await Document.updateOne({ _id }, update).session(session);

// ✅
// react-force-graph-3d는 nodes/links가 가변 ref여야 하지만,
// React strict mode에서 두 번 호출돼 좌표가 튀는 이슈가 있어 useMemo로 고정.
const graphData = useMemo(() => ({ nodes, links }), [nodes, links]);
```

### 6.2 나쁜 주석 (WHAT — 코드가 이미 말함)

```typescript
// ❌
let count = 0;  // count를 0으로 초기화

// ❌
function getUser() {  // 사용자를 가져옴
  return user;
}
```

### 6.3 TODO 주석

```typescript
// TODO(BE1): 그래프 노드 수 100개 초과 시 LOD 적용 — W4 마무리
// FIXME(FE1): autocomplete 드롭다운 키보드 위쪽 이동 안 됨
// NOTE: 본 함수는 트랜잭션 안에서만 호출돼야 함
```

→ TODO는 `(담당자명)` 또는 GitHub Issue 번호 포함. 방치된 TODO 금지.

### 6.4 JSDoc (공개 API만)

```typescript
/**
 * 노트 내용에서 @/#/& 링크를 파싱하여 노드/관계 배열을 반환합니다.
 *
 * @example
 * parse("@엄마 #감정 &Child")
 * // → { mentions: ['엄마'], tags: ['감정'], pac: [{ role: 'Child' }] }
 */
export function parse(content: string): ParseResult { ... }
```

---

## 7. 테스트

### 7.1 파일 위치

- 단위 테스트: `<file>.test.ts` (같은 디렉터리)
- 통합 테스트: `Backend/tests/` 또는 `Front/tests/`

### 7.2 테스트 작성 패턴 (AAA)

```typescript
describe('linkParser', () => {
  it('parses @ mentions correctly', () => {
    // Arrange
    const input = '오늘 @엄마 와 통화함';

    // Act
    const result = parse(input);

    // Assert
    expect(result.mentions).toEqual(['엄마']);
  });

  it('returns empty arrays for plain text', () => {
    expect(parse('그냥 텍스트')).toEqual({
      mentions: [],
      tags: [],
      pac: [],
      relationships: [],
    });
  });
});
```

### 7.3 describe/it 네이밍

- `describe('컴포넌트/함수명', ...)`
- `it('should ... when ...', ...)` 또는 한글로 자연스럽게

---

## 8. 보안

### 8.1 절대 커밋 금지

- `.env`, `.env.local`
- API 키, JWT 시크릿
- 사용자 데이터 덤프
- 비밀번호 (평문이든 해시든)

### 8.2 입력 검증

- 모든 외부 입력은 zod로 검증
- SQL Injection (Mongoose 사용 시 자동 방지)
- XSS: React가 기본 escape, `dangerouslySetInnerHTML` 사용 시 sanitize-html
- CSRF: SameSite 쿠키 또는 토큰

### 8.3 로깅 시 민감 정보 제외

```typescript
// ❌
logger.info('Login', { email, password });

// ✅
logger.info('Login', { email });
```

---

## 9. Git 메시지 (요약, 상세는 [Git 가이드](./Git_협업_가이드.md))

```
feat(editor): add @ mention autocomplete dropdown
fix(graph): prevent crash when nodes array is empty
docs(api): update note PUT endpoint examples
```

---

